{
  "name": "fastapi-example",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "tags": [],
  "// targets": "to see all targets run: nx show project fastapi-example --web",
  "targets": {
    "serve": {
      "command": "uv run python -m fastapi_example",
      "options": {
        "cwd": "examples/fastapi/src"
      }
    }
  }
}
